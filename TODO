- [ ] Fine grained segments for accessing MMIO instead of a flat segment.
- [ ] If the PFA is slow, add external bitmap-like metadata for page order and availability status.
- [ ] Data segment nullptr guard using paging.

## If ever needed.
- [ ] Replace fixed max gdt (512 entries) with a dynamically allocated gdt and gdt bitmap.
